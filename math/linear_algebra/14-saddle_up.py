#!/usr/bin/env python3


"""Useless comment"""

import numpy as np


def np_matmul(
        first_matrix: np.ndarray,
        second_matrix: np.ndarray
) -> np.ndarray:
    """
    Call the np matmul -- Seems to be a useless function bruh
    :param first_matrix: The first matrix
    :param second_matrix: The second matrix
    :return: The matmul
    """
    return np.matmul(first_matrix, second_matrix)
